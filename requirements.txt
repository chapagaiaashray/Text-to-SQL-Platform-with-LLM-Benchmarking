# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Database ----
psycopg[binary]==3.2.13          # PostgreSQL driver (psycopg v3, modern API)
SQLAlchemy==2.0.36              # used later for metadata-DB ORM / connection pooling

# ---- LLM providers (Week 2) ----
openai==1.59.6
anthropic==0.42.0
google-genai==0.3.0
# Llama is accessed via local Ollama HTTP API (no SDK pinned here)

# ---- RAG stack (Week 6) ----
chromadb==0.5.23
sentence-transformers==3.3.1

# ---- Benchmark / data wrangling (Weeks 4-5) ----
pandas==2.2.3
numpy==2.2.1

# ---- Utilities ----
python-dotenv==1.0.1
httpx==0.28.1
tenacity==9.0.0                 # retry/backoff for flaky LLM API calls
rich==13.9.4                    # pretty CLI output for the explore/load scripts

# ---- Dev / test ----
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.6
